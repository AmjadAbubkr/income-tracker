---
phase: 09-release-safety-and-data-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/types.ts
  - src/utils/currency.ts
  - src/utils/database.ts
  - src/utils/storage.ts
  - src/context/AuthContext.tsx
  - src/App.tsx
  - src/stores/incomeStore.ts
  - src/stores/expenseStore.ts
  - src/stores/subscriptionStore.ts
  - src/utils/backup.ts
  - src/components/SalesPage.tsx
  - src/components/IncomeEntryForm.tsx
  - src/components/AnalyticsPage.tsx
  - src/components/ExpensesPage.tsx
  - src/components/SubscriptionsPage.tsx
  - tests/*
---

<objective>
Make the offline app safe to release by repairing the strict TypeScript
baseline, replacing floating-point persisted money, isolating local profiles,
and making the linked financial writes transactional and test-covered.
</objective>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Establish a strict verification baseline and repair current component contracts</name>
<files>package.json, TypeScript-incompatible component callers, tests setup</files>
<action>
Add non-watch `test` and `typecheck` scripts and configure the existing Vitest
tooling for browser IndexedDB tests. Reconcile the current form/chart/card
contracts in SalesPage, ExpensesPage, AnalyticsPage, and SubscriptionsPage;
do not redesign their UI. Add focused rendering tests covering the repaired
routes.
</action>
<verify>`npm run typecheck` exits 0; `npm test -- --run` exits 0.</verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Make money exact and migrate existing local records</name>
<files>src/types.ts, src/utils/currency.ts, src/utils/database.ts, all money callers, tests</files>
<action>
Represent every persisted amount as integer minor units, centralize decimal
input parsing and display formatting, and upgrade IndexedDB without discarding
existing records. Update calculations, exports, subscriptions, backups, and
forms to use the shared conversion helpers. Test decimal edge cases and a
schema migration using existing float records.
</action>
<verify>Money conversion and migration tests pass; source has no `parseFloat`
or direct persisted monetary floating-point arithmetic.</verify>
</task>

<task type="auto" tdd="true">
<name>Task 3: Enforce local profile boundaries</name>
<files>src/utils/database.ts, src/utils/storage.ts, src/context/AuthContext.tsx, src/App.tsx, query hooks/stores, tests</files>
<action>
Replace bare persisted user-ID restoration with a random local session record,
clear QueryClient and Zustand data on logout/account changes, key user-scoped
queries by user ID, and make update/delete operations verify current-user
ownership in their IndexedDB transaction. Test two users switching accounts
and attempting a cross-user mutation.
</action>
<verify>Account-switch and ownership tests pass; stale products, income,
expenses, and subscriptions cannot render or mutate for the next user.</verify>
</task>

<task type="auto" tdd="true">
<name>Task 4: Commit financial workflows atomically</name>
<files>src/utils/database.ts, src/utils/storage.ts, stores, SalesPage, IncomeEntryForm, backup.ts, tests</files>
<action>
Create storage-level multi-store transactions for checkout, due-subscription
processing, and backup restore. Checkout validates/decrements tracked stock and
writes all sales together. Subscription processing writes generated ledger
entries with schedule advancement idempotently. Restore validates all data
before one transaction writes it. Preserve ISO date-only records and define an
end-of-month billing rule.
</action>
<verify>Tests cover insufficient stock, failed multi-item checkout, repeated
subscription runs, overdue catch-up, month-end billing, invalid backup, and
successful full restore. `npm run typecheck` and `npm test -- --run` pass.</verify>
</task>

</tasks>

<success_criteria>
- Strict TypeScript succeeds and all current screen contracts are valid.
- Financial data is exact, user-scoped, and transactionally durable.
- Scheduled subscription income and expenses persist once only.
- A failed restore or checkout does not partially change data.
- Automated tests protect the critical paths.
</success_criteria>
