---
phase: 10-complete-remaining-v1-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/utils/reporting.ts
  - src/utils/export.ts
  - src/components/Dashboard.tsx
  - src/components/AnalyticsPage.tsx
  - src/components/AnalyticsCharts.tsx
  - src/components/PrintReport.tsx
  - src/App.css
  - tests/reporting.test.ts
  - tests/screen-contracts.test.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - RPT-01
  - RPT-02
  - RPT-05
  - RPT-06
---

<objective>
Make dashboard, analytics, Excel, and print reporting use one correct period
calculation contract so revenue, expenses, net profit, charts, activity, and
exports agree with each other.
</objective>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Centralize period summaries and activity calculations</name>
<files>src/utils/reporting.ts, src/components/Dashboard.tsx, src/components/AnalyticsPage.tsx, tests/reporting.test.ts</files>
<action>
Create a small pure reporting utility for daily, monthly, and all-time date
selection. It should return filtered income/expense records, revenue,
expenses, net profit, item totals, top products, and a latest-first activity
list using integer minor units. Replace duplicated/inconsistent calculations in
Dashboard and AnalyticsPage, including the current all-expenses bug. Keep the
existing view toggles and empty states, but make their labels and selected
period behavior explicit. Add tests for date boundaries, mixed income/expense
periods, top-product ranking, and recent activity ordering.
</action>
<verify>`npx.cmd vitest run tests/reporting.test.ts` passes; `npm run typecheck`
passes.</verify>
<acceptance_criteria>
- Daily, monthly, and all-time views apply the same date rules everywhere.
- Summary cards show revenue, expenses, and net profit from the selected period.
- Recent activity includes latest sales and expenses in deterministic order.
- Reporting calculations never use floating-point persisted money.
</acceptance_criteria>
</task>

<task type="auto" tdd="true">
<name>Task 2: Complete dashboard charts and analytics surfaces</name>
<files>src/components/Dashboard.tsx, src/components/AnalyticsPage.tsx, src/components/AnalyticsCharts.tsx, src/App.css, tests/screen-contracts.test.tsx</files>
<action>
Pass the centralized period summary into the existing dashboard and analytics
components. Extend chart props so revenue and expenses are both represented,
keep top-product ranking visible, and add the recent activity feed with
translated labels and useful empty states. Ensure displayed currency values are
formatted from minor units and chart/tooltips do not expose raw minor-unit
integers to users.
</action>
<verify>`npx.cmd vitest run tests/screen-contracts.test.tsx` passes; manually
switch daily/monthly/all-time views and confirm cards, chart, ranking, and
activity change together; `npm run typecheck` passes.</verify>
<acceptance_criteria>
- Dashboard and analytics satisfy all five DASH requirements.
- Revenue-vs-expenses chart includes both series for the selected period.
- Top products and latest activity reflect the same selected period.
- EN/FR/AR labels and Arabic layout remain usable.
</acceptance_criteria>
</task>

<task type="auto" tdd="true">
<name>Task 3: Correct Excel exports and add the print PDF report flow</name>
<files>src/utils/export.ts, src/components/AnalyticsPage.tsx, src/components/PrintReport.tsx, src/App.css, tests/reporting.test.ts</files>
<action>
Update exportToExcel to accept the selected period's income and expense data
and summary, generate daily/monthly workbook reports with an expenses sheet
and period summary, and keep filenames/date labels consistent. Add a focused
print report surface that renders the selected financial summary, period
details, and compact activity/table content with print-only CSS; expose a
Print/Save as PDF action from analytics without adding a PDF package. Test the
export input calculations and print report contract without relying on a real
browser download.
</action>
<verify>`npx.cmd vitest run tests/reporting.test.ts` passes; `npm run build`
passes; inspect the print stylesheet and verify the browser print dialog can
save the report as PDF in a local manual check.</verify>
<acceptance_criteria>
- Daily and monthly Excel exports contain only the selected period's income,
  expenses, and summary values.
- Period summary report shows revenue, expenses, and net profit.
- Analytics offers an offline print-ready report that can be saved as PDF.
- No new PDF dependency is added.
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npx.cmd vitest run tests/reporting.test.ts tests/screen-contracts.test.tsx`
- `npm run typecheck`
- `npm run build`
- `git diff --check`
</verification>

<success_criteria>
- Dashboard and analytics calculations agree for every supported period.
- Excel exports are complete and period-correct.
- The print report is usable as an offline PDF workflow.
</success_criteria>
