---
phase: 10-complete-remaining-v1-workflows
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/context/AuthContext.tsx
  - src/context/NotificationContext.tsx
  - src/components/Header.tsx
  - src/components/SettingsPage.tsx
  - src/components/Sidebar.tsx
  - src/components/SalesPage.tsx
  - src/components/ExpensesPage.tsx
  - src/components/ProductsPage.tsx
  - src/components/SubscriptionsPage.tsx
  - src/translations.ts
  - src/App.css
  - tests/profile-boundaries.test.tsx
  - tests/notifications-and-localization.test.tsx
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - SET-04
---

<objective>
Finish the shared-shell quality of the remaining workflows: refresh and
translate in-app low-stock/subscription notifications, allow safe local email
profile editing, and remove user-facing hardcoded copy from the Phase 10
surfaces in all three supported languages.
</objective>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Make notifications localized, current, and actionable</name>
<files>src/context/NotificationContext.tsx, src/components/Header.tsx, src/components/SalesPage.tsx, src/components/ExpensesPage.tsx, src/components/ProductsPage.tsx, src/components/SubscriptionsPage.tsx, tests/notifications-and-localization.test.tsx</files>
<action>
Keep the existing in-app notification provider and thresholds, but represent
notification data separately from translated display copy. Expose a refresh
method and call it after product stock, sales, and subscription mutations so
the header does not require a reload to become current. Replace hardcoded
notification/dropdown labels, actions, and confirmations on the touched screens
with translation keys. Keep notification navigation restricted to the existing
View union and preserve accessible button semantics.
</action>
<verify>`npx.cmd vitest run tests/notifications-and-localization.test.tsx` passes;
manually create a low-stock product and near-term subscription, then confirm
both appear in the dropdown and navigate to the correct view; `npm run
typecheck` passes.</verify>
<acceptance_criteria>
- Low-stock alerts appear at stock <= 5 and subscription reminders within three
  days.
- Alerts refresh after relevant local mutations without a page reload.
- Notification content and shared dropdown controls render in EN, FR, and AR.
- Interactive notification controls are keyboard-accessible.
</acceptance_criteria>
</task>

<task type="auto" tdd="true">
<name>Task 2: Complete local profile name and email editing</name>
<files>src/context/AuthContext.tsx, src/components/SettingsPage.tsx, tests/profile-boundaries.test.tsx</files>
<action>
Enable email editing in the existing settings form and pass the changed email
through AuthContext.updateProfile. Normalize and validate the address before
save, reject a duplicate belonging to another local user without changing the
current profile, and preserve password/session/profile isolation. Translate
the validation, duplicate, success, and failure messages.
</action>
<verify>`npx.cmd vitest run tests/profile-boundaries.test.tsx` passes for a
successful update, duplicate rejection, and cross-user isolation; `npm run
typecheck` passes.</verify>
<acceptance_criteria>
- Authenticated users can edit and persist name and email locally.
- Duplicate email updates fail safely and leave the original profile intact.
- The current session remains valid after a successful profile update.
- Existing Phase 9 account-boundary tests continue to pass.
</acceptance_criteria>
</task>

<task type="auto" tdd="true">
<name>Task 3: Audit visible Phase 10 copy across EN, FR, and AR</name>
<files>src/translations.ts, src/components/Header.tsx, src/components/Sidebar.tsx, src/components/SettingsPage.tsx, src/components/SalesPage.tsx, src/components/ExpensesPage.tsx, src/components/Dashboard.tsx, src/components/AnalyticsPage.tsx, src/components/SubscriptionsPage.tsx, tests/notifications-and-localization.test.tsx</files>
<action>
Inventory visible strings in the shared shell and Phase 10 screens, add focused
translation keys with complete English/French/Arabic values, and replace
hardcoded headings, buttons, filter labels, empty states, report labels,
confirmations, and error messages. Preserve existing interpolation tokens and
Arabic RTL direction. Add render-level assertions for representative copy in
each language so future missing translations are caught even though the current
translation type is permissive.
</action>
<verify>`npx.cmd vitest run tests/notifications-and-localization.test.tsx tests/profile-boundaries.test.tsx`
passes; `npm run typecheck`; `npm run build`; `git diff --check`.</verify>
<acceptance_criteria>
- No user-facing hardcoded English remains in the Phase 10 workflow surfaces
  where a translation key is available.
- Representative dashboard, history, settings, notification, and report copy
  renders in EN/FR/AR.
- Arabic retains RTL document direction and readable controls.
</acceptance_criteria>
</task>

</tasks>

<verification>
- `npx.cmd vitest run tests/notifications-and-localization.test.tsx tests/profile-boundaries.test.tsx`
- `npx.cmd vitest run`
- `npm run typecheck`
- `npm run build`
- `git diff --check`
</verification>

<success_criteria>
- Notifications are current, localized, and navigable.
- Profile name/email editing is safe across local accounts.
- Phase 10 shared-shell and workflow copy has EN/FR/AR coverage.
</success_criteria>
